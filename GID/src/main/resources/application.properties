spring.application.name=GID

# ----------------------------------
# PUERTO DEL SERVIDOR (CRUCIAL PARA RENDER)
# ----------------------------------
# Render asigna un puerto en la variable PORT. Si no existe, usa 8080.
# --- PUERTO (OBLIGATORIO PARA RENDER) ---
server.port=${PORT:8080}

# --- DATABASE ---
spring.datasource.url=${DB_URL:jdbc:postgresql://ep-restless-bush-ahtyfyr4-pooler.c-3.us-east-1.aws.neon.tech:5432/neondb?sslmode=require}
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_7QHIaED0FUzB}

# ----------------------------------
# CONFIGURACIÓN DE JPA (HIBERNATE)
# ----------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# HikariCP (Pool de conexiones optimizado para nube)
spring.datasource.hikari.maximum-pool-size=3
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=10000
spring.datasource.hikari.leak-detection-threshold=60000

# Hibernate Platform
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ----------------------------------
# SWAGGER / OPENAPI
# ----------------------------------
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/doc/swagger-ui.html

# ----------------------------------
# OTROS AJUSTES
# ----------------------------------
spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

# Actuator (Health checks para Render)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always

